apiVersion: apps/v1beta1
kind: Deployment
metadata:
  labels:
    app: gl-ui
  name: gl-ui-deployment
  namespace: default
spec:
  minReadySeconds: 5
  replicas: 2
  selector:
    matchLabels:
      app: gl-ui
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: gl-ui
    spec:
      containers:
      - image: gcr.io/oceanic-isotope-199421/github-zmad5306-gl-ui:8ec0b14161e4d3e6c43845240cb91d638840b014
        name: gl-ui
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: 256Mi
          requests:
            memory: 128Mi
